<!-- markdownlint-disable MD013 MD041 -->

## 1. Baseline, Inventory, and Assignment

- [ ] 1.1 [Owner: migration-coordinator | Depends: none] Re-run a read-only inventory of all catalog items and record exact item/mode counts, every `<slug>:<mode-id>` missing `after_copy`, every fill-pointer value, default-mode coverage, mode-ID duplicates, evaluation coverage, item `order`, index membership, lane mismatch, orphan, and unresolved-reference state.
- [ ] 1.2 [Owner: migration-coordinator | Depends: 1.1] Compare the apply-time inventory with the design baseline of 83 items, 91 modes, 38 prompt-bearing modes missing `after_copy`, 42 `eval_required: true`, 41 absent booleans, 83 item `order` fields, and zero production no-paste modes; stop for classification if it differs.
- [ ] 1.3 [Owner: migration-coordinator | Depends: none] Inventory every non-generated reader, schema, validator, type, emitter, checker, route projection, fixture, test, and UI consumer of `order`, `eval_required`, `evaluation`, `after_copy`, `fill_pointer`, `template_omission_reason`, `placeholders`, and `prompt_slugs`.
- [ ] 1.4 [Owner: migration-coordinator | Depends: 1.3] Explicitly classify both `catalog/fixtures/**` and `packages/catalog-core/test/fixtures/**`, `web/scripts/emit-seo.mjs`, `web/scripts/emit-seo.test.mjs`, `routes-from-catalog.mjs`, `route-descriptors.js`, `scripts/catalog_badge_data.mjs`, `scripts/catalog_constants.py`, `scripts/check_readme_recipes.py`, and `tests/test_catalog_research_upgrade_2026_07_25.py` in the migration matrix.
- [ ] 1.5 [Owner: migration-coordinator | Depends: 1.1, 1.3] Freeze the writer map: one disjoint lane owner per `catalog/items/*.yaml` set; one owner each for schema contracts, package phases, parity oracle, README emitter/checker, web interactions, SEO/discovery, shared site-data integration, generated publication, and independent verification.
- [ ] 1.6 [Owner: migration-coordinator | Depends: 1.5] Publish a reviewer checklist for prompt-specific Expected output, Upgrade when, evaluation policy/rationale, structured check IDs and branch fields, observable pass criteria, placeholder alignment, evidence, caveat, and safety; reject generic bulk-fill or vague gate prose.

## 2. Disjoint Catalog Content Migration

- [ ] 2.1 [Owner: content-research | Depends: 1.2, 1.6] Prepare the nine Research items: author reviewed structured evaluation objects and complete mode metadata, while retaining `eval_required` and item `order` only until the coordinated cutover.
- [ ] 2.2 [Owner: content-writing | Depends: 1.2, 1.6] Prepare the nine Writing items, including all apply-time `after_copy` backfills and authored structured evaluation objects, while retaining legacy fields only until the coordinated cutover.
- [ ] 2.3 [Owner: content-coding | Depends: 1.2, 1.6] Prepare the six Coding items with reviewed structured evaluation objects and complete strict mode branches, while retaining legacy fields only until the coordinated cutover.
- [ ] 2.4 [Owner: content-data | Depends: 1.2, 1.6] Prepare the ten Data items, including all apply-time `after_copy` backfills and authored structured evaluation objects, while retaining legacy fields only until the coordinated cutover.
- [ ] 2.5 [Owner: content-product | Depends: 1.2, 1.6] Prepare the six Product items with reviewed structured evaluation objects and complete strict mode branches, while retaining legacy fields only until the coordinated cutover.
- [ ] 2.6 [Owner: content-operations | Depends: 1.2, 1.6] Prepare the six Operations items with reviewed structured evaluation objects and complete strict mode branches, while retaining legacy fields only until the coordinated cutover.
- [ ] 2.7 [Owner: content-agents | Depends: 1.2, 1.6] Prepare the sixteen Agents items, including all apply-time `after_copy` backfills, authored structured evaluation objects, and preserved agent-specific safety semantics, while retaining legacy fields only until the coordinated cutover.
- [ ] 2.8 [Owner: content-reasoning | Depends: 1.2, 1.6] Prepare the twenty-one Reasoning items, including all apply-time `after_copy` backfills and known non-default cases plus authored structured evaluation objects, while retaining legacy fields only until the coordinated cutover.
- [ ] 2.9 [Owner: content-reviewer | Depends: 2.1, 2.2, 2.3, 2.4, 2.5, 2.6, 2.7, 2.8] Review every changed mode and evaluation branch against prompt text, placeholders, evidence, safety, and caveats; return prompt-specific defects to the owning lane only.
- [ ] 2.10 [Owner: migration-coordinator | Depends: 2.9] Re-run the preparation inventory and require 83 explicit policy-valid structured evaluation objects, zero duplicate per-item mode IDs, the exact fill-pointer token on every paste mode, `placeholders: []` on every no-paste mode, and zero incomplete/mixed/empty branches; record every still-present `eval_required` and item `order` location for the coordinated cutover instead of removing it early.

## 3. Public and Runtime Record Contracts

- [ ] 3.1 [Owner: schema-contract | Depends: 1.4] Update the public Draft 2020-12 item schema with trimmed nonblank semantics, strict paste/no-paste and structured acceptance-check unions, the exact fill-pointer token, `placeholders: []` omission branch, structural mode-ID constraints, and rejection of legacy fields; document that property-based mode-ID uniqueness is outside portable JSON Schema.
- [ ] 3.2 [Owner: schema-contract | Depends: 1.4] Update the public index schema for unique lane order and all record-local index constraints needed before package validation.
- [ ] 3.3 [Owner: schema-contract | Depends: 3.1, 3.2] Update Zod item/index schemas and runtime record refinements to match public structural rules and enforce each later duplicate mode ID at `/modes/<index>/id`, while accepting the same ID in different items.
- [ ] 3.4 [Owner: schema-contract | Depends: 3.1, 3.2, 3.3] Migrate contract fixtures in `catalog/fixtures/**` and existing record fixtures in `packages/catalog-core/test/fixtures/**` without moving parity-oracle fixture ownership into these trees.
- [ ] 3.5 [Owner: schema-contract | Depends: 3.4, 2.10] Add focused cases for blank-after-trimming values, alternate fill pointers, populated/missing no-paste placeholders, duplicate mode IDs, all valid structured evaluation branches, bare-string/duplicate-ID/incomplete/mixed checks, multiline commands, non-`exit_zero` expectations, empty checks, and retired fields.
- [ ] 3.6 [Owner: schema-contract | Depends: 3.5] Prove public-schema and Zod/runtime acceptance and diagnostic-path behavior, explicitly proving the public adapter is not claimed for property-based mode-ID uniqueness, before strict record enforcement is merged into the integrated working state.

## 4. Raw, Validated, and Canonical Package Boundary

- [ ] 4.1 [Owner: canonical-package | Depends: 1.4] Define explicit raw, validated, and canonical package representations; raw loading retains index provenance plus every item file, filename stem, parsed value, and parse/identity diagnostic.
- [ ] 4.2 [Owner: canonical-package | Depends: 4.1, 3.3] Implement complete-set validation without index projection, map deduplication, filtering, sorting, or unresolved-reference skipping before diagnostics are complete.
- [ ] 4.3 [Owner: canonical-package | Depends: 4.2] Emit stable package codes and repository-relative `documentPath` plus RFC 6901 `instancePath` for duplicate lane order, duplicate/missing/unlisted/orphan/wrong-lane prompt membership, duplicate per-item mode IDs, and unresolved references.
- [ ] 4.4 [Owner: canonical-package | Depends: 4.3] Ensure failed validation returns diagnostics and raw provenance but no canonical value or generator-safe package.
- [ ] 4.5 [Owner: canonical-package | Depends: 4.4] Implement canonicalization as a function that accepts only successful validated packages, sorts lanes by unique lane `order`, and materializes each lane's authored `prompt_slugs` sequence.
- [ ] 4.6 [Owner: canonical-package | Depends: 4.5] Make generator-facing APIs accept only canonical packages and reject raw/failed values before opening or writing any output.
- [ ] 4.7 [Owner: canonical-package | Depends: 4.6] Add multi-defect raw-set tests proving all applicable membership/reference defects remain observable together and no canonical package is produced.
- [ ] 4.8 [Owner: canonical-package | Depends: 4.6] Add scrambled-enumeration tests proving identical canonical package order regardless of filesystem or filename order.
- [ ] 4.9 [Owner: canonical-package | Depends: 4.6] Preserve `featured_prompt_slugs` as an explicit curated-order/ranking exception: validate every featured slug against the canonical package and retain the exact authored subset sequence even when it differs from canonical relative order, without treating it as a second full-catalog authority.

## 5. Executable Validator-Parity Oracle

- [ ] 5.1 [Owner: parity-oracle | Depends: 3.6, 4.6] Create `packages/catalog-core/test/validator-parity/manifest.js`, shared builders, owner-supplied domain-module registration seams, the checked invariant-to-adapter coverage matrix, and adapters plus `packages/catalog-core/test/validator-parity.test.js`; reserve parity-oracle-authored cases for the non-overlapping core domains.
- [ ] 5.2 [Owner: parity-oracle | Depends: 5.1] Add required adapters for public item/index JSON Schema, Zod item/index schemas, package semantics, the canonical-package generator boundary, Python README semantics, README freshness, structured site-data comparison, and SEO/route/discovery/full-Markdown semantics.
- [ ] 5.3 [Owner: parity-oracle | Depends: 5.2] Normalize every adapter result to `{adapter, fixtureId, accepted, diagnostics, observations}` with stable code, repository-relative/logical `documentPath`, RFC 6901 `instancePath`, message, and context.
- [ ] 5.4 [Owner: parity-oracle | Depends: 5.3] Suppress generic union wrappers when specific child diagnostics exist, deduplicate exact diagnostics, and sort by document path, instance path, code, then canonicalized context.
- [ ] 5.5 [Owner: parity-oracle | Depends: 5.4] Compare complete diagnostic arrays and declared observations; fail on uncovered matrix cells, case-level omission of a required adapter, skipped adapters, missing/extra diagnostics, acceptance disagreement, path/code/context drift, or unexpected output opens/writes.
- [ ] 5.6 [Owner: parity-oracle | Depends: 5.5] Add record-item and record-index cases for all mode, fill-pointer, structured-check, evaluation, trimming, order, and retired-field branches; mark public JSON Schema non-applicable only for the named property-based duplicate-mode-ID case.
- [ ] 5.7 [Owner: parity-oracle | Depends: 5.6, 4.7, 4.8] Add package cases for complete-set defects, multi-diagnostic reporting, unresolved references, canonicalization blocking, duplicate mode IDs, and scrambled input.
- [ ] 5.8 [Owner: parity-oracle | Depends: 5.6, 6.8, 7.7, 9.6] Register and execute the README artifact/freshness, site-data artifact, and SEO/route/discovery/full-Markdown case modules supplied by their projection owners after all required diagnostic interfaces stabilize; retain ownership only of the shared framework, matrix integration, and non-overlapping core cases, and do not author or duplicate artifact-owner cases or fixtures.
- [ ] 5.9 [Owner: parity-oracle | Depends: 5.5, 5.7] Make every raw/failed-package generator-boundary case execute the guard and prove zero output opens and zero output writes.
- [ ] 5.10 [Owner: parity-oracle | Depends: 5.6, 5.7, 5.8, 5.9] Add `pnpm catalog:parity` and require the complete coverage matrix in focused and full validation gates.

## 6. README Emitter and Semantic Checker

- [ ] 6.1 [Owner: readme-projection | Depends: 2.10, 4.6] Make the README emitter accept only the canonical package and remove independent order reconstruction.
- [ ] 6.2 [Owner: readme-projection | Depends: 6.1] Centralize the machine token projection and render exactly ``Match the **placeholder table** above; paste `none` for optional zones you omit.`` without exposing `match_placeholder_table`.
- [ ] 6.3 [Owner: readme-projection | Depends: 6.2] Render exactly one copy fence, Fill, Expected output, and Upgrade when for paste-path defaults; render `No copyable template for this mode` plus the authored reason and no `After copy` summary for no-paste defaults.
- [ ] 6.4 [Owner: readme-projection | Depends: 6.3] Render policy/rationale for every prompt; preserve each structured check's ID, type, branch fields, and authored order in required checklists and optional recommended checks; fabricate nothing for recommended/no-check or `not_required` branches.
- [ ] 6.5 [Owner: readme-projection | Depends: 6.4] Update `scripts/check_readme_recipes.py` to parse exact-one field/section cardinality, policy branches, structured-check IDs/types/fields/order, omission state, raw-token absence, and canonical Prompt Index/Library order.
- [ ] 6.6 [Owner: readme-projection | Depends: 6.5] Replace the literal `eval_required:` assertion in `tests/test_catalog_research_upgrade_2026_07_25.py` with parsed assertions over required command/observation checks, recommended-with-checks, recommended-without-checks, and not-required output.
- [ ] 6.7 [Owner: readme-projection | Depends: 6.5] Emit stable missing/duplicate/order/policy diagnostic codes with normalized logical artifact paths; include line numbers only as context and first expected/actual slug in order-drift context.
- [ ] 6.8 [Owner: readme-projection | Depends: 6.7] As the README artifact-domain writer, add parity cases, fixtures, and emitter/checker mutations for missing and duplicate fields, no-paste cards, all evaluation branches, raw sentinel exposure, and membership-complete order drift; expose them for parity-oracle registration without transferring case or fixture ownership.

## 7. SEO and Discovery Projection

- [ ] 7.1 [Owner: seo-discovery | Depends: 2.10, 4.6] Migrate `web/scripts/emit-seo.mjs` to canonical prompt input and remove reads/exports of prompt `order` and `eval_required`.
- [ ] 7.2 [Owner: seo-discovery | Depends: 7.1] Render evaluation policy/rationale plus every structured check's ID, type, branch fields, and authored order, explicit omission reasons, and the shared human fill-pointer projection without exposing `match_placeholder_table`.
- [ ] 7.3 [Owner: seo-discovery | Depends: 7.2] Migrate `web/scripts/emit-seo.test.mjs` fixtures away from legacy fields and incomplete paste modes; cover command/observation checks, required, recommended, not-required, no-paste, canonical order, and nested fence text.
- [ ] 7.4 [Owner: seo-discovery | Depends: 7.1] Reconcile `routes-from-catalog.mjs` and `route-descriptors.js` so route inventory remains its own route-order authority while prompt serialization consumes canonical prompt order.
- [ ] 7.5 [Owner: seo-discovery | Depends: 7.4] Verify non-ranking discovery projections preserve canonical prompt-relative order and any named relevance/analytical projection uses canonical rank as its final prompt tie-breaker.
- [ ] 7.6 [Owner: seo-discovery | Depends: 7.3, 7.5, 7.7] Run focused SEO, route, sitemap, `llms.txt`, and `llms-full.txt` tests and require zero retired-field or raw-sentinel output.
- [ ] 7.7 [Owner: seo-discovery | Depends: 7.2, 7.4] As sole writer of SEO, route/discovery, `llms.txt`, and full-Markdown parity cases and fixtures, add catalog-alone cases plus stable normalized artifact diagnostics and mutation helpers for retired fields, structured-check loss, raw-sentinel exposure, fabricated paste guidance, and non-ranking prompt-order drift. Expose those modules for parity-oracle registration without transferring ownership; when source provenance is co-applied, accept its delegated source-aware overlap in the same owner lane without making the catalog-alone cases depend on that change.

## 8. Accessible Web Interactions and Fixture Build

- [ ] 8.1 [Owner: web-interactions | Depends: 2.10, 4.6] Update checked TypeScript catalog types for strict mode branches, evaluation policies, and structured command/observation checks before changing renderers.
- [ ] 8.2 [Owner: web-interactions | Depends: 8.1] Update `PromptPage.tsx` so no-paste modes retain mode selector, functional Copy link, Print, Evaluation, Safety, Sources, and Caveats while paste-only controls are absent from the DOM; Copy link must copy only the canonical prompt-page URL with at most the normalized selected-mode query, never a prompt body, filled prompt, or provider payload.
- [ ] 8.3 [Owner: web-interactions | Depends: 8.2] Render the exact persistent no-paste heading/body outside any live region and use only the existing atomic polite status for exact transition announcements; initial direct no-paste load leaves it empty.
- [ ] 8.4 [Owner: web-interactions | Depends: 8.1] Update `CatalogPreviewModal.tsx` with the same persistent no-paste section and no prompt copy block, paste action, or actionable placeholder display.
- [ ] 8.5 [Owner: web-interactions | Depends: 8.3, 8.4] Add unit/accessibility tests for exact control presence/DOM absence, initial silence, one announcement per transition, paste-mode recovery, and keyboard order; prove no-paste Copy link writes only the canonical prompt-page URL while mode selection and every removed paste action write no prompt body, filled prompt, or provider payload and open no provider.
- [ ] 8.6 [Owner: web-interactions | Depends: 3.6, 4.6] Create `web/tests/fixtures/catalog-no-paste/catalog/` with one complete paste mode and one no-paste mode containing nonblank omission reason, `placeholders: []`, and no `prompt`/`after_copy`.
- [ ] 8.7 [Owner: web-interactions | Depends: 8.6] Validate the fixture through public schema, Zod/runtime, and package semantics, then generate its full/companion pair and entry module under `web/.tmp/catalog-no-paste/data/` through catalog-core.
- [ ] 8.8 [Owner: web-interactions | Depends: 8.7] Route all full/companion catalog imports through `@catalog-data`; implement one checked exact-specifier target selector consumed by Vite and a Node 24 ESM unit-test preload resolver, map ordinary profiles to checked production data, map only the explicit generated-and-validated `catalog-no-paste` test profile to `web/.tmp/catalog-no-paste/data/catalog-data.mjs`, and fail closed for unknown, ordinary/publication fixture, direct-target, or bypass requests. Add resolver contract tests that import a real catalog consumer under ordinary and fixture profiles without Vite and reject unauthorized profiles.
- [ ] 8.9 [Owner: web-interactions | Depends: 8.8] Add root command `pnpm web:test:no-paste-fixture` to generate/validate the fixture, run the focused native-Node unit/accessibility suite with the preload resolver selecting the exact fixture entry, and invoke a checked Vite wrapper that forces `--mode catalog-no-paste --outDir dist-catalog-no-paste`, resolves the effective path before Vite starts, and fails unless it is exactly `web/dist-catalog-no-paste` and distinct from `web/dist` despite config, environment, symlink, or passthrough overrides. Serve only that verified directory and run `web/tests/playwright.catalog-no-paste.config.mjs` without mutating checked generated JSON or `web/dist`.
- [ ] 8.10 [Owner: web-interactions | Depends: 8.5, 8.9, 8.12] Run unit, accessibility, and isolated browser tests over real prompt-detail and preview interactions, including a no-paste Copy link assertion for canonical-URL-only clipboard output and negative assertions for prompt-body, filled-prompt, provider-payload, and provider-open side effects.
- [ ] 8.11 [Owner: web-interactions | Depends: 8.1] Update paste-path prompt detail and preview rendering to consume the centralized plain-text fill projection and render evaluation policy/rationale plus every structured check ID, type, branch field, and authored order.
- [ ] 8.12 [Owner: web-interactions | Depends: 8.5, 8.11] Add unit/accessibility tests for the exact shared paste Fill copy and required/recommended/not-required rendering across command, observation, present-check, and absent-check branches.

## 9. Shared Site-Data and Source-Provenance Integration

- [ ] 9.1 [Owner: site-data-integration | Depends: 2.10, 4.6] Preserve the existing one-lock, journal, staging, candidate/backup, recovery, permission, directory-sync, and two-file publication transaction unchanged.
- [ ] 9.2 [Owner: site-data-integration | Depends: 9.1] Make `catalog.json` own full public collections and canonical prompts while `catalog-meta.json` owns only compact metadata, aggregates, matching supported version, and `catalog_sha256`; define the catalog-alone digest as 64-character lowercase SHA-256 over the exact final `catalog.json` bytes, including its schema/version field, and do not infer payload symmetry from transaction symmetry.
- [ ] 9.3 [Owner: site-data-integration | Depends: 9.2] When `source-provenance-site-data-bridge` is co-applied, wait only for its tasks 0.1 and 7.10; then, as sole implementation writer of the shared emitter, comparator, pair tests, and pure projections, reconcile both projections without waiting for source task 8.12 or the final coexistence task and without regenerating the checked pair. This catalog change may land alone first, but the source change may not land or archive first.
- [ ] 9.4 [Owner: site-data-integration | Depends: 9.3] Emit prompt evaluation and complete mode branches in canonical order; remove prompt `order`, `eval_required`, `facet`, recipe/pattern arrays, pattern sections, and recipe/pattern counts; deterministically serialize the final full artifact once, compute `catalog_sha256` after its bytes are fixed, and pass those same bytes plus the companion projection to the existing pair publisher without writing checked outputs in this workstream.
- [ ] 9.5 [Owner: site-data-integration | Depends: 9.4] Implement a structured freshness comparator that first verifies the companion's `catalog_sha256` against the exact committed `catalog.json` bytes and rejects parse-equivalent reserialization or digest/version disagreement; then require both `generated_at` values to be present, valid under the supported timestamp contract, and exactly equal before reusing that common value in expected projections or ignoring its paired volatility. Derive each artifact's distinct expected projection, compare every other nonvolatile field/array order and cross-file count/version invariant, and delegate source-specific checks to the source-provenance contract.
- [ ] 9.6 [Owner: site-data-integration | Depends: 9.5] As the site-data artifact-domain writer, add parity cases, fixtures, and prompt-only, catalog-alone exact-byte digest, source-only, cross-file integrity/version, canonical-order, retired-field, and asymmetric-payload mutations with stable normalized diagnostics. Include missing, malformed, and individually valid but unequal paired `generated_at` cases, proving timestamp volatility is ignored only after both values validate and exactly match. Expose these modules for parity-oracle registration without transferring ownership. When the source change is co-applied, consume its task 8.12 integration-readiness result and task 8.13 final coexistence result after catalog task 9.4; do not regenerate the checked pair.
- [ ] 9.7 [Owner: site-data-integration | Depends: 9.6] Re-run crash, contention, rollback, recovery, permission, symlink, interrupted-publication, and non-mutating-check suites unchanged except for their explicit new payload projections.

## 10. Remaining Consumer Migration and Ranking Audit

- [ ] 10.1 [Owner: migration-coordinator | Depends: 2.10, 4.6] Migrate `scripts/catalog_badge_data.mjs` and `scripts/catalog_constants.py` to canonical package counts/order and remove retired-field assumptions.
- [ ] 10.2 [Owner: ranking-auditor | Depends: 4.8] Classify home lane/search filters, landing lane groups, command-palette filtering/truncation, and Explore prompt scope/query/cluster projections as non-ranking and assert canonical relative order.
- [ ] 10.3 [Owner: ranking-auditor | Depends: 4.8, 4.9] Classify `featured_prompt_slugs` as the explicit curated-order/ranking exception and preserve its exact authored subset sequence without making it a full-catalog authority; separately classify search-score, hub, degree, and co-citation projections as analytical rankings, document their score keys/directions, and require canonical rank as their final prompt tie-breaker.
- [ ] 10.4 [Owner: ranking-auditor | Depends: 10.2, 10.3] Prevent generic search helpers from applying non-canonical prompt tie-breakers in production, while retaining unrelated non-prompt behavior through a prompt-specific comparator or non-production boundary.
- [ ] 10.5 [Owner: migration-coordinator | Depends: 5.10, 6.6, 6.8, 7.6, 8.10, 9.7, 10.1, 10.4] Freeze coordinated-cutover readiness: every replacement reader, renderer, checker, adapter, Python assertion, and generated projection is complete; legacy fields remain only in the lane-owned source records and are no longer read.
- [ ] 10.6 [Owner: content-research | Depends: 10.5] Remove `eval_required` and item `order` from the nine prepared Research items without changing reviewed replacement data.
- [ ] 10.7 [Owner: content-writing | Depends: 10.5] Remove `eval_required` and item `order` from the nine prepared Writing items without changing reviewed replacement data.
- [ ] 10.8 [Owner: content-coding | Depends: 10.5] Remove `eval_required` and item `order` from the six prepared Coding items without changing reviewed replacement data.
- [ ] 10.9 [Owner: content-data | Depends: 10.5] Remove `eval_required` and item `order` from the ten prepared Data items without changing reviewed replacement data.
- [ ] 10.10 [Owner: content-product | Depends: 10.5] Remove `eval_required` and item `order` from the six prepared Product items without changing reviewed replacement data.
- [ ] 10.11 [Owner: content-operations | Depends: 10.5] Remove `eval_required` and item `order` from the six prepared Operations items without changing reviewed replacement data.
- [ ] 10.12 [Owner: content-agents | Depends: 10.5] Remove `eval_required` and item `order` from the sixteen prepared Agents items without changing reviewed replacement data.
- [ ] 10.13 [Owner: content-reasoning | Depends: 10.5] Remove `eval_required` and item `order` from the twenty-one prepared Reasoning items without changing reviewed replacement data.
- [ ] 10.14 [Owner: migration-coordinator | Depends: 10.6, 10.7, 10.8, 10.9, 10.10, 10.11, 10.12, 10.13] Repeat source and consumer inventory and require zero `eval_required`, zero item prompt `order`, zero retired-field readers, zero independent fill instructions, zero filename/title prompt ordering, zero silent unresolved-reference skipping, and zero unclassified prompt rankings.
- [ ] 10.15 [Owner: schema-contract | Depends: 3.6, 10.14] Run strict schemas and record/package semantics against the integrated production catalog, remove any migration-only permissive test seam, and require the coordinated cutover to pass before generation.

## 11. Canonical Generated Publication

- [ ] 11.1 [Owner: generated-publication | Depends: 4.8, 5.10, 6.8, 7.6, 8.10, 9.7, 10.15] Run strict source validation and the complete `pnpm catalog:parity` coverage matrix; stop before generation on any failure. When `source-provenance-site-data-bridge` is co-applied, this validation also consumes that change's task 12.3 publication handoff (validated projection inputs and inspection checklist) before the checked-pair generation path proceeds; this catalog change may still land alone first.
- [ ] 11.2 [Owner: generated-publication | Depends: 11.1] Generate `README.md` only through the badge-aware transactional README command; no other owner writes it.
- [ ] 11.3 [Owner: generated-publication | Depends: 11.1] As the sole checked-pair writer, accept publication delegated by `source-provenance-site-data-bridge` when co-applied and generate `web/src/data/catalog.json` plus `web/src/data/catalog-meta.json` only through the canonical paired site-data command, publishing the exact full-artifact bytes hashed into `catalog_sha256`; no other owner writes the pair.
- [ ] 11.4 [Owner: generated-publication | Depends: 11.2] Inspect README semantic diff lane by lane for prompt-specific post-copy copy, evaluation branches, no-paste labeling, raw-sentinel absence, exact-one cardinality, and canonical order.
- [ ] 11.5 [Owner: generated-publication | Depends: 11.3] Inspect each site-data artifact against its own projection for canonical prompts, source-provenance integration when present, matching version, valid and exactly equal paired `generated_at` values, exact-byte `catalog_sha256`, compact metadata, and no unrelated generated churn.
- [ ] 11.6 [Owner: generated-publication | Depends: 11.4, 11.5] Run non-mutating README and site-data freshness checks and require byte/structured parity without further writes.
- [ ] 11.7 [Owner: generated-publication | Depends: 7.6, 11.6] Treat generated sitemap, route shells, `llms.txt`, `llms-full.txt`, and `web/dist` as ephemeral build outputs; verify them through the build and never hand-edit or commit them unless repository policy explicitly designates a checked artifact.

## 12. Independent Verification and Closeout

- [ ] 12.1 [Owner: independent-verifier | Depends: 11.7] Run catalog-core schema, validator-parity, package, README emitter/checker, site-data, and transaction/recovery suites under Node 24.x and pnpm 11.21.0.
- [ ] 12.2 [Owner: independent-verifier | Depends: 11.7] Run Python README contract and repository unittest suites, including parsed evaluation branches and stable artifact diagnostics.
- [ ] 12.3 [Owner: independent-verifier | Depends: 11.7] Run web typecheck, unit, SEO/discovery, route, prompt-detail, preview, ranking, and isolated no-paste Playwright suites.
- [ ] 12.4 [Owner: independent-verifier | Depends: 12.1, 12.2, 12.3] Run formatting, lint, Markdown lint, links, JSON/YAML parsing, whitespace, generated badge/chrome, focused strict OpenSpec, repository-wide strict OpenSpec, and task-DAG reference/cycle/full-ancestry checks.
- [ ] 12.5 [Owner: independent-verifier | Depends: 12.4] Run the deterministic fast validation gate and full repository validation gate; retain exact output for any failure and do not mark the migration complete with skipped required checks.
- [ ] 12.6 [Owner: independent-verifier | Depends: 12.5] Confirm all items/modes survive generation, each prompt appears exactly once under its authored lane, all canonical prompt surfaces agree, and counts remain source-derived.
- [ ] 12.7 [Owner: independent-verifier | Depends: 12.6] Confirm no compatibility reader, dual evaluation path, new taxonomy/route/dependency, production sentinel record, raw machine token exposure, or unscoped third prompt-order authority was added, while the authored `featured_prompt_slugs` sequence remains only the explicit featured-projection ranking exception.
- [ ] 12.8 [Owner: migration-coordinator | Depends: 12.7] Record the final inventory, normalized parity evidence, generated write/freshness evidence, exact verification commands, and proof that all 104 non-closeout tasks are transitive ancestors of this closeout task before requesting review, commit, or deployment authorization.
