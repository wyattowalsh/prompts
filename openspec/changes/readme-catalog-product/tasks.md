<!-- markdownlint-disable MD013 MD041 -->

## 1. Emitter

- [ ] 1.1 Drop `navBadges()` from `emitRecipeCard`; keep `#top` in preamble (T034).
- [ ] 1.2 Update J-03 so it does not require per-card `#top` badges.
- [ ] 1.3 Optional above-fence `none` reminder without a second Fill heading (T036).
- [ ] 1.4 Optional Prompt Index equality-lint vs `index.yaml` (T035).

## 2. Chrome

- [ ] 2.1 Add Takumi JSX templates and `scripts/render_readme_chrome.mjs`.
- [ ] 2.2 Commit four PNGs and hash-check scripts (`catalog:readme-chrome` / `:check`).
- [ ] 2.3 Pin `takumi-js@2.9.2` and `@takumi-rs/core@2.9.2`.

## 3. Generate

- [ ] 3.1 One `pnpm catalog:readme` after shell/YAML/chrome sources exist (T062).
