<!-- markdownlint-disable MD013 MD041 -->

## 1. Emitter

- [x] 1.1 Drop `navBadges()` from `emitRecipeCard`; keep `#top` in preamble (T034).
- [x] 1.2 Update J-03 so it does not require per-card `#top` badges.
- [x] 1.3 Optional above-fence `none` reminder without a second Fill heading (T036).
- [x] 1.4 Prompt Index equality-lint vs `index.yaml` (T035).
- [x] 1.5 Agents-lane one-line safety above the fence (RV-S-005).
- [x] 1.6 Stub JS ShieldCN URL builders that Python last-writes (RV-S-002).

## 2. Chrome

- [x] 2.1 Add Takumi JSX templates and `scripts/render_readme_chrome.mjs`.
- [x] 2.2 Commit four PNGs and hash-check scripts (`catalog:readme-chrome` / `:check`).
- [x] 2.3 Pin `takumi-js@2.9.2` and `@takumi-rs/core@2.9.2`.
- [x] 2.4 README Quality runs hash-only `pnpm catalog:readme-chrome:check`.

## 3. Generate

- [x] 3.1 One `pnpm catalog:readme` after shell/YAML/chrome sources exist (T062).
