<!-- markdownlint-disable MD013 MD041 -->

> **Status:** Specification only. Every unchecked item below is future work and
> requires its own implementation or live-change authorization. Completing this
> OpenSpec revision performs no production code, workflow, repository-rule,
> provider-setting, credential, deployment, domain, commit, push, install, or
> lockfile mutation.

## 1. Read-only control-plane, trust, and authority discovery

- [ ] 1.1 [Owner: discovery-coordinator | Depends: none] Capture the current authenticated GitHub `main` SHA and repository ID, rules and bypass inventory, workflow/check/App identities, Vercel project/environment settings, production domain, current deployment ID/URL/SHA, preview behavior, rollback eligibility, and every principal or automatic path that can assign production, without mutating either service.
- [ ] 1.2 [Owner: quality-provenance | Depends: 1.1] Determine the authenticated GitHub API fields that bind repository/head-repository IDs, event, tested/head/base refs including absence, workflow ID/path and executed definition revision, check suite/run, source App, run/attempt, SHA, timestamps, and conclusion; reject candidate artifacts, outputs, summaries, environment values, and unsigned webhooks as authoritative provenance.
- [ ] 1.3 [Owner: route-assurance | Depends: 1.1] Inventory the build-owned typed route inventory and current `catalog-contract-completeness-ordering` status; define the canonical release-manifest serialization, schema version, digest boundary, candidate binding, and prior-release rollback lookup without adding a second route list.
- [ ] 1.4 [Owner: discovery-coordinator | Depends: 1.2, 1.3] Verify on the actual hosting account/project whether host-managed protection can bind the protected authenticated quality result and route digest to the exact deployment SHA, consume a fence-bound one-operation assignment authorization, hold later candidates during verification/recovery, emit authenticated immutable assignment evidence, constrain provider bypass, and support event-plus-watchdog recovery.
- [ ] 1.5 [Owner: recovery-authority | Depends: 1.4] Verify the host-managed path has a coherent post-assignment verifier and either provider-native rollback or a dedicated project-scoped recovery principal technically restricted to the checkpointed deployment; reject code conventions, read-before-write checks, or broad promotion credentials as fence or authority boundaries.
- [ ] 1.6 [Owner: discovery-coordinator | Depends: 1.2, 1.3] Verify the alternative GitHub Actions-owned path can build and deploy the exact checked-out SHA through a sole credential-holding mutation gateway that enforces fence epochs, idempotency, and expected mappings while independent host production assignment is disabled and previews remain available.
- [ ] 1.7 [Owner: release-ledger | Depends: 1.2] Verify an authenticated append-only durable store can enforce record-type writer ACLs, unpredictable operation nonces, previous-digest and monotonic-version compare-and-append, stale-fence rejection, exact-retry idempotency, conflicting-replay rejection, authoritative server timestamps, retention, and deletion/overwrite denial.
- [ ] 1.8 [Owner: discovery-coordinator | Depends: 1.4, 1.5, 1.6, 1.7] Record the selected single normal-writer control plane, host or gateway fence architecture, bounded recovery actor and trigger, separated ledger writers, distinct bypass roles, authority matrix, and evidence for every selection criterion; stop with no live changes if neither path satisfies the complete contract.

## 2. Protected exact-SHA quality provenance

- [ ] 2.1 [Owner: quality-provenance | Depends: 1.2] Add focused tests that reject SHA mismatches, pull-request and synthetic-merge results, manual same-SHA runs, fork heads, wrong or self-reported refs/events, skipped/canceled/failed conclusions, ambiguous check names, unexpected Apps, unauthenticated payloads, stale runs, and unanchored workflow definitions.
- [ ] 2.2 [Owner: quality-workflow | Depends: 1.2] Ensure every `main` commit the production control plane can deploy receives the full required quality result independent of path filtering, and expose only identifiers needed for the independent authenticated collector to resolve repository, event/ref, workflow definition, check, App, run/attempt, SHA, timestamp, and conclusion provenance.
- [ ] 2.3 [Owner: quality-provenance | Depends: 2.1, 2.2] Define one stable workflow ID/path and required job identity, protect an approved executable workflow-definition revision/digest from ordinary candidate and deployment-automation changes, and implement a distinct reviewed anchor-rotation path whose changing candidate cannot qualify retroactively.
- [ ] 2.4 [Owner: quality-provenance | Depends: 1.7, 2.3] Implement the independent authenticated GitHub provenance collector and quality-attestation ledger writer; bind repository IDs, protected workflow digest, source App/check identities, event/refs, run/attempt, SHA, conclusion, and route-manifest version/digest under its record-type-only principal.
- [ ] 2.5 [Owner: release-inspector | Depends: 1.8, 2.4] Add a non-mutating dry-run inspector that resolves authenticated current `main`, protected quality attestation, immutable deployment metadata, route-manifest identity, proposed fence/checkpoint, writer authority, recovery trigger, bypass records, and rollback target without production or authoritative ledger-write credentials.

## 3. Immutable route-manifest and route verification

- [ ] 3.1 [Owner: route-assurance | Depends: 1.3, 2.3] Generate the canonical release route manifest from the typed route inventory with schema version, release SHA, deterministic ordered entries/kinds, redirects, indexability, unsupported probes, discovery and metadata expectations, and canonical SHA-256 digest; verify source parity in the protected quality workflow.
- [ ] 3.2 [Owner: route-assurance | Depends: 2.4, 3.1] Bind the candidate manifest version/digest to authenticated quality evidence and immutable deployment metadata, then implement bounded candidate and canonical-origin checks against those exact bytes rather than current `main` or a regenerated inventory.
- [ ] 3.3 [Owner: route-assurance | Depends: 2.4, 3.1] Require the rollback deployment to have prior successful protected quality evidence, its own immutable manifest version/digest, and a fresh passing canonical check against that same manifest; allow bootstrap evidence only when it satisfies identical facts.
- [ ] 3.4 [Owner: route-assurance | Depends: 1.7, 3.2, 3.3] Append machine-readable candidate, production, rollback-qualification, and restoration observations under the active fence with operation/checkpoint IDs, phase, release/deployment/manifest identities, status, location, canonical metadata, discovery origins, timestamp, and deterministic/transient failure classification.

## 4. Append-only checkpoint, fencing, autonomous recovery, and repair

- [ ] 4.1 [Owner: release-ledger | Depends: 1.7, 2.4, 3.4] Implement authenticated append-only schemas and record-type ACLs for quality attestations, checkpoints/state intents other than `recovery-recorded`, provider observations, route observations, the fence-authority-only terminal `recovery-recorded` record, and derived summaries/indexes; prove candidate, preview, release-controller, and recovery-controller principals cannot author record types outside their assigned authority.
- [ ] 4.2 [Owner: fence-authority | Depends: 4.1] Implement repository-and-domain-scoped strictly increasing fence epochs with conditional acquisition and closure, operation nonces, previous-digest and next-version compare-and-append, exact-retry idempotency, conflicting-duplicate rejection, and stale-epoch rejection; make the fence authority the sole executor of fence closure and the sole writer that can atomically append `recovery-recorded` while closing its matching epoch after validating a condition-bound controller request.
- [ ] 4.3 [Owner: provider-mutation | Depends: 1.8, 4.2] Implement the selected provider mutation boundary: either host consumption of a one-operation authorization or a sole credential-holding gateway. Bind operation/epoch, candidate or rollback target, route digest, expected prior mapping, checkpoint digest, idempotency key, expiry, and current-main proof; reject stale/replayed/conflicting requests.
- [ ] 4.4 [Owner: provider-observer | Depends: 1.8, 4.1, 4.2, 4.3] Implement a separate authenticated provider/domain observer and provider-observation ledger writer with no provider-mutation, checkpoint, state-intent, route-observation, `recovery-recorded`, or fence-closure authority; bind each observation to authenticated provider identity, operation, epoch, checkpoint, deployment, canonical mapping, event/replay identity, and authoritative timestamp, and reject workflow output or mutation-principal self-report as provider truth.
- [ ] 4.5 [Owner: release-controller | Depends: 2.5, 4.2, 4.3, 4.4] Revalidate authenticated current `main`, quality attestation, observer-authenticated provider mapping, and checkpoint immediately before assignment authorization; append terminal `superseded` and request conditional fence closure from the fence authority when `main` changed before assignment, but require verification or rollback once assignment may have begun.
- [ ] 4.6 [Owner: release-controller | Depends: 4.1, 4.2, 4.3, 4.4, 4.5] Implement the durable `prepared` → `checkpointed` → `superseded`/assignment/verification/publication → success-or-rollback → mode-qualified `failed-recovery` state machine and the condition-bound request path to terminal `recovery-recorded`, persisting each controller-owned state intent before its external side effect, distinguishing `restoration-complete` from `restoration-incomplete`, and reserving the `recovery-recorded` append and fence closure for the fence authority.
- [ ] 4.7 [Owner: recovery-trigger | Depends: 1.8, 4.6] Implement authenticated provider-event ingestion plus an independent durable watchdog for every nonterminal operation; checkpoint fixed scan interval, trigger-start deadline, retries, backoff, overall bound, replay window, deduplication key, and escalation destination.
- [ ] 4.8 [Owner: recovery-controller | Depends: 4.4, 4.6, 4.7] Implement idempotent restart reconciliation from authenticated ledger state, active fence, and provider-observer truth, making assignment uncertainty converge to verification or rollback and making rollback states or `restoration-incomplete` failed recovery resume only same-operation, same-epoch checkpoint-target restoration without forward authority.
- [ ] 4.9 [Owner: recovery-controller | Depends: 4.8] Implement the deterministic failure table: pre-checkpoint abort, immediate rollback when authoritative post-assignment state is unavailable/corrupt, bounded derived-publication retries only when complete authoritative facts exist, rollback-first behavior during restoration, `restoration-incomplete` failed recovery when rollback or restored-origin verification fails, and `restoration-complete` failed recovery after verified restoration with missing derived evidence.
- [ ] 4.10 [Owner: recovery-controller | Depends: 4.2, 4.6, 4.9] Implement idempotent evidence repair only for `restoration-complete` failed recovery, reverify observer-authenticated checkpointed mapping and manifest-bound route health, and submit a condition-bound closure request carrying the expected operation, epoch, state version, and record digest; do not grant the controller `recovery-recorded` append or fence-closure authority, and keep `restoration-incomplete` in same-epoch checkpoint-target recovery until `restored` is appended and verified.
- [ ] 4.11 [Owner: release-contract-tests | Depends: 4.3, 4.4, 4.5, 4.6, 4.7, 4.8, 4.9, 4.10] Add tests for stale fences at every durable/provider boundary, provider-observer authentication and writer separation, rejection of mutation-principal self-report, conflicting idempotency bodies, nonce and event replay, supersession at the assignment boundary, host-writer blocking, lost provider events, watchdog bounds, post-assignment crash, verification failure, every publication-failure row, restart during rollback, restoration-incomplete rollback failure and same-epoch checkpoint-target retry, controller rejection when attempting `recovery-recorded` append or fence closure, fence-authority-only atomic closure, restoration-complete duplicate repair, lost closure acknowledgement, and drift-blocked fence release.

## 5. Distinct repository and provider bypass governance

- [ ] 5.1 [Owner: repository-governance | Depends: 2.3] Prepare the exact `main` ruleset definition: pull requests for normal changes, strict current-head required quality check from the expected App, protected workflow-definition and trust-anchor paths, blocked deletion and force pushes, and no deployment-automation bypass.
- [ ] 5.2 [Owner: repository-governance | Depends: 5.1] Define the narrow maintainer-only repository-ingress bypass with actor/reason/ref/SHA/anchor-digest evidence; prove it grants no provider credential, production authorization, or quality qualification and that the resulting commit still needs protected authenticated post-merge quality.
- [ ] 5.3 [Owner: provider-mutation | Depends: 4.3, 4.6] Define a separate production-maintainer provider-mutation bypass limited to a protected authenticated exact-SHA deployment with matching route digest or the checkpointed known-good target; require the active fence, expected mapping, verification, and evidence and grant no repository-ingress authority.
- [ ] 5.4 [Owner: release-contract-tests | Depends: 5.2, 5.3] Prove ordinary automation has neither bypass and that repository-ingress evidence cannot authorize provider mutation, provider-bypass evidence cannot update `main` or the workflow anchor, and unvalidated/context-mismatched targets remain ineligible.
- [ ] 5.5 [Owner: repository-governance | Depends: 5.4] Obtain fresh explicit maintainer authorization before creating or changing repository rules or the protected workflow anchor; capture before/after evidence and verify normal rejection and authorized repository-ingress behavior without destructive branch operations.

## 6. Pre-cutover rehearsal

- [ ] 6.1 [Owner: rehearsal-coordinator | Depends: 1.8, 3.4, 4.11, 5.4] Build an isolated non-canonical rehearsal target that cannot receive the production domain but exercises the selected normal writer, host/gateway fence boundary, authenticated ledger writers, provider observer, route verifier, autonomous triggers, repairer, and bounded rollback executor with production-equivalent policy.
- [ ] 6.2 [Owner: rehearsal-coordinator | Depends: 6.1] Rehearse a protected authenticated exact-SHA candidate through manifest binding, fenced assignment, canonical-equivalent verification, complete evidence publication, successful fence closure, rollback to a rehearsal known-good release using its own manifest, and re-promotion under a higher epoch.
- [ ] 6.3 [Owner: rehearsal-coordinator | Depends: 6.1] Rehearse current-main supersession immediately before assignment plus stale-epoch, previous-digest, conflicting-idempotency, nonce-replay, workflow-anchor, and authenticated-provenance rejection at every durable and provider boundary.
- [ ] 6.4 [Owner: rehearsal-coordinator | Depends: 6.1] Rehearse controller cancellation after assignment, loss/replay of the provider event, bounded watchdog recovery, trigger-start exhaustion with retained fence/escalation, restart during rollback, and `restoration-incomplete` rollback failure followed only by separately authorized same-operation, same-epoch checkpoint-target recovery with forward authority explicitly absent and premature closure rejected.
- [ ] 6.5 [Owner: rehearsal-coordinator | Depends: 6.1] Rehearse bounded publication exhaustion, verified restoration, `restoration-complete` failed recovery, duplicate evidence repair, lost fence-closure acknowledgement, and mapping/manifest/health drift that prevents fence release.
- [ ] 6.6 [Owner: rehearsal-coordinator | Depends: 6.1] Rehearse a deliberately interrupted control-plane configuration change and prove partial replacement authority is removed before the prior configuration and known-good deployment are restored.
- [ ] 6.7 [Owner: independent-verifier | Depends: 6.2, 6.3, 6.4, 6.5, 6.6] Block production-writer retirement until all rehearsal evidence independently confirms preview isolation, protected provenance, writer separation, fence participation, anti-replay, autonomous recovery bounds, state recovery, route manifests, bypass separation, repair, rollback, and partial-cutover restoration.

## 7. Single-writer production cutover

- [ ] 7.1 [Owner: cutover-coordinator | Depends: 5.5, 6.7] Obtain fresh explicit maintainer authorization before changing provider settings, GitHub environments/secrets, production-authority workflows, repository rules, ledger/fence principals, bypass roles, or the production domain.
- [ ] 7.2 [Owner: cutover-coordinator | Depends: 3.3, 4.2, 7.1] Freeze release mutations and durably capture the prior provider configuration, every production-capable principal, canonical mapping, qualified known-good deployment/quality/manifest identity, restoration sequence, and expected first fence epoch before changing authority.
- [ ] 7.3 [Owner: cutover-coordinator | Depends: 7.2] Retire the previous path's production-domain assignment authority, then run a controlled ordinary-trigger probe proving it can at most create preview/non-production output and cannot change the canonical mapping; retain configuration, denial/audit, and unchanged-domain evidence.
- [ ] 7.4 [Owner: cutover-coordinator | Depends: 7.3] Enable only the selected normal writer, monotonic fence/provider boundary, authenticated separated ledger writers, autonomous event/watchdog recovery, rollback-only actor, and distinct provider bypass; reject any host writer outside the fence or broad GitHub recovery credential.
- [ ] 7.5 [Owner: cutover-coordinator | Depends: 7.2, 7.3, 7.4] Verify successful cutover or, on any partial failure, remove replacement authority first, restore the prior provider configuration and qualified known-good release from the durable snapshot, verify its manifest-bound canonical routes, and record a failed cutover rather than retaining dual writers.
- [ ] 7.6 [Owner: cutover-coordinator | Depends: 7.5] Retain pull-request and branch previews without production or authoritative ledger credentials and add recurring drift proof that retired writers, preview principals, ordinary automation, and stale fence epochs cannot assign production.

## 8. Controlled production assurance and recovery

- [ ] 8.1 [Owner: production-exercise | Depends: 7.5] Exercise one staged candidate through protected authenticated production-context quality, immutable manifest verification, fence/checkpoint publication, assignment-bound current-main proof, production assignment, canonical verification, complete evidence publication, and conditional fence closure.
- [ ] 8.2 [Owner: production-exercise | Depends: 7.5] Exercise fixed-bound derived-publication recovery and prove bound exhaustion deterministically transitions to rollback rather than operator-selected success or fence release.
- [ ] 8.3 [Owner: production-exercise | Depends: 7.5] Exercise cancellation after assignment, provider-event loss, watchdog recovery, trigger-start exhaustion, and restart during rollback; confirm later host/GitHub candidates remain blocked while the fence is active.
- [ ] 8.4 [Owner: production-exercise | Depends: 8.1] Exercise a controlled fenced rollback to the qualified known-good deployment, verify its SHA, prior protected quality evidence, immutable route manifest, and route contract, then re-promote the validated candidate under a new higher epoch.
- [ ] 8.5 [Owner: production-exercise | Depends: 8.2, 8.3, 8.4] Exercise verified-restoration `restoration-complete` failed recovery, idempotent duplicate repair, lost closure acknowledgement, successful `recovery-recorded` fence closure, and mapping/manifest/health drift that retains the fence.
- [ ] 8.6 [Owner: production-exercise | Depends: 5.4, 7.5] Verify repository-ingress and provider-mutation bypasses remain unavailable to ordinary automation, use separate maintainer identities/credentials/evidence, and cannot be substituted for each other.
- [ ] 8.7 [Owner: independent-verifier | Depends: 7.6, 8.1, 8.5, 8.6] Confirm authenticated GitHub provenance, protected workflow definition, provider deployment metadata, immutable route digest, canonical domain, fence/state ledger, provider/route observations, distinct bypass evidence, and final release or recovery record identify one coherent candidate and prior known-good chain.

## 9. Documentation, conditional migration dependency, and closeout

- [ ] 9.1 [Owner: operations-documentation | Depends: 8.7] Document the selected control plane, protected workflow anchor and rotation, authenticated quality provenance, host/gateway fence, separated ledger writers, autonomous recovery bounds, immutable route manifests, state and evidence decision tables, bypass separation, cutover restoration, retired-writer proof, failed-recovery repair, fence closure, and rollback runbook without secret values.
- [ ] 9.2 [Owner: independent-verifier | Depends: 9.1] Run focused release-contract tests, workflow/config syntax and immutable-pin checks, ledger/fence/trigger tests, route-manifest and production-route assurance, strict OpenSpec validation, the complete deterministic repository gate, formatting, and whitespace checks.
- [ ] 9.3 [Owner: closeout-coordinator | Depends: 1.3, 9.2] Re-evaluate `catalog-contract-completeness-ordering` at closeout and record whether it is applied/archived, still active and able to change route-manifest behavior, or demonstrably independent of the implemented route-manifest interface.
- [ ] 9.4 [Owner: route-assurance | Depends: 9.3] If the catalog change is active or co-applied and can alter canonical routes, discovery, inventory generation, or hashing, wait for its apply/archive and revalidate final manifest version/digest binding through protected quality, deployment, candidate, production, and rollback evidence; otherwise record the reviewed not-applicable basis without adding compatibility behavior.
- [ ] 9.5 [Owner: closeout-coordinator | Depends: 9.4] Keep this change active until live rules, selected provider configuration, rehearsal, retired-writer negative proof, exact-SHA protected provenance, fence/anti-replay evidence, autonomous recovery drills, rollback and repair drills, documentation, strict validation, and the conditional catalog closeout gate have been independently verified; only then archive through the repository OpenSpec process.
