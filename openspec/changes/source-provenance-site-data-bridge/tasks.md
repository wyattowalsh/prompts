<!-- markdownlint-disable MD013 MD041 -->

> Specification-only plan: these unchecked tasks describe future implementation. This change does not edit production code, tests, source authoring data, generated artifacts, deployment settings, or lockfiles.

Every task carries machine-parseable `[Owner: <owner> | Depends: <dependency-list>]` metadata. Internal dependencies use source task ids; cross-change dependencies use `<change-id>:<task-id>`. Task 12.8 is the sole closeout node and its ancestry check MUST cover every other source task. The same graph includes these parseable cross-change edges:

```yaml
cross_change_edges:
  - source-provenance-site-data-bridge:0.1 -> catalog-contract-completeness-ordering:9.3
  - source-provenance-site-data-bridge:7.10 -> catalog-contract-completeness-ordering:9.3
  - catalog-contract-completeness-ordering:9.4 -> source-provenance-site-data-bridge:8.13
  - catalog-contract-completeness-ordering:9.4 -> source-provenance-site-data-bridge:10.9
  - source-provenance-site-data-bridge:8.12 -> catalog-contract-completeness-ordering:9.6
  - source-provenance-site-data-bridge:8.13 -> catalog-contract-completeness-ordering:9.6
  - catalog-contract-completeness-ordering:9.7 -> source-provenance-site-data-bridge:12.3
  - source-provenance-site-data-bridge:12.3 -> catalog-contract-completeness-ordering:11.1
  - catalog-contract-completeness-ordering:11.6 -> source-provenance-site-data-bridge:12.4
  - catalog-contract-completeness-ordering:11.6 -> source-provenance-site-data-bridge:12.5
  - catalog-contract-completeness-ordering:11.6 -> source-provenance-site-data-bridge:12.6
```

## 0. Enforce the Integration Prerequisite

- [ ] 0.1 [Owner: integration-preflight | Depends: none] Add a repository-owned apply/archive preflight that determines and records exactly one base-state mode: `independent-base-proven` when proof covers every added or modified requirement in the complete accepted `catalog-contract-completeness-ordering` delta already present in the base specifications rather than a bounded-metadata sentinel or subset, and `combined-active` when that proof is absent. `independent-base-proven` alone permits independent source apply/archive/publication; under `combined-active` the integrated implementation graph may proceed, but no source-first apply/archive/publication is authorized and no atomic co-archive is claimed.
- [ ] 0.2 [Owner: integration-coordination | Depends: 0.1] Record and enforce non-overlapping owners for the combined result: `site-data-integration` alone writes the shared emitter/comparator integration; `seo-discovery` alone writes `web/scripts/emit-seo.mjs`, `web/scripts/emit-seo.test.mjs`, overlapping SEO and route/discovery adapters, full-Markdown serializers, and every fixture/parity surface for those outputs; and the prerequisite change's `generated-publication` owner alone regenerates `web/src/data/catalog.json` and `web/src/data/catalog-meta.json`. Refuse a source-side checked-pair writer.

## 1. Lock Source Contracts and Fixture Vocabulary

- [ ] 1.1 [Owner: source-contracts | Depends: 0.2] Add a strict source-manifest runtime schema and checked-in JSON Schema for stable ids, display titles, primary/replacement URLs, owner, source type, split lifecycle/link status, three required nullable source-review dates, optional claim reviews, and optional approved public claim scope.
- [ ] 1.2 [Owner: source-contracts | Depends: 1.1] Add schema/runtime parity tests that permute object keys and exercise Unicode, nullable keys, unknown-field rejection, and every enum value.
- [ ] 1.3 [Owner: source-contracts | Depends: 1.2] Add fixture-local structural manifests for cited and uncited sources, prompt-level and mode-level citations, alternate contextual titles, canonical-primary-URL mutations, redirects without replacements, editorial replacements, null review dates, claim-review/public-scope record shapes, annotations, and malformed fingerprint syntax. Do not stamp accepted fingerprints before production projection code exists.
- [ ] 1.4 [Owner: source-contracts | Depends: 1.3] Define the fixture-helper interface and dependency boundary so later positive fixtures can call production claim projection, canonical serialization, and fingerprinting directly; prohibit test-only production-equivalent hashing or accepted placeholder fingerprints.
- [ ] 1.5 [Owner: source-contracts | Depends: 1.4] Define stable diagnostic-code fixtures and expected per-code file/instance payloads for `SOURCE_SCHEMA_INVALID`, `SOURCE_ID_DUPLICATE`, `SOURCE_URL_INVALID`, `SOURCE_URL_AMBIGUOUS`, `SOURCE_CITATION_UNMATCHED`, `SOURCE_CITATION_DUPLICATE`, `SOURCE_CITATION_FIELDS_INVALID`, `SOURCE_CLAIM_REVIEW_INVALID`, `SOURCE_CLAIM_REVIEW_TARGET_INVALID`, `SOURCE_CLAIM_REVIEW_STALE`, `SOURCE_PUBLIC_CLAIM_SCOPE_INVALID`, and `SOURCE_PUBLIC_CLAIM_SCOPE_STALE`.
- [ ] 1.6 [Owner: source-contracts | Depends: 1.5] Define the incremented full generated-catalog schema and bounded metadata-companion schema, including exact citation/source shapes, required null keys, matching version/timestamp, and full-artifact digest.

## 2. Centralize Conservative URL Identity

- [ ] 2.1 [Owner: source-identity | Depends: 1.6] Implement one pure catalog-core source URL validator/normalizer that requires already-trimmed credential-free absolute HTTPS input and uses standards-compliant URL serialization.
- [ ] 2.2 [Owner: source-identity | Depends: 2.1] Add positive URL tests for host/scheme case, IDNA serialization, default HTTPS ports, dot-segment resolution, and percent encoding.
- [ ] 2.3 [Owner: source-identity | Depends: 2.2] Add non-equivalence tests proving path case, non-root trailing slash, fragment, query value, and query ordering remain identity-significant.
- [ ] 2.4 [Owner: source-identity | Depends: 2.3] Add negative fixtures for whitespace, relative URLs, non-HTTPS schemes, credentials, malformed hosts, invalid ports, and invalid percent encoding, expecting `SOURCE_URL_INVALID`.
- [ ] 2.5 [Owner: source-identity | Depends: 2.4] Route source primary URLs, replacement URLs, prompt/mode citations, manifest uniqueness checks, source-refresh comparisons, and coverage checks through the same normalizer or its compiled output.
- [ ] 2.6 [Owner: source-identity | Depends: 2.5] Prove by fixture that redirects, `www` differences, query cleanup, tracking-parameter removal, and replacement URLs never create implicit equivalence.
- [ ] 2.7 [Owner: source-identity | Depends: 2.6] Retire or wrap duplicated Python URL logic only after every existing source-refresh and manifest-coverage fixture passes against catalog-core's authority.

## 3. Validate Independent Status and Review Dimensions

- [ ] 3.1 [Owner: source-evidence | Depends: 2.7] Implement `lifecycle_status` validation for only `unreviewed`, `retained`, `stale`, and `replaced`.
- [ ] 3.2 [Owner: source-evidence | Depends: 3.1] Implement `link_status` validation for only `not_checked`, `reachable`, `redirected`, `blocked`, and `unavailable`.
- [ ] 3.3 [Owner: source-evidence | Depends: 3.2] Require presence of `inventory_reviewed_on`, `link_checked_on`, and `content_reviewed_on` while accepting only ISO calendar dates (`YYYY-MM-DD`) or null values.
- [ ] 3.4 [Owner: source-evidence | Depends: 3.3] Reject `link_status: not_checked` with a non-null link date and reject every other link status with a null link date.
- [ ] 3.5 [Owner: source-evidence | Depends: 3.4] Reject non-`unreviewed` lifecycle values with a null inventory-review date while allowing `unreviewed` with null.
- [ ] 3.6 [Owner: source-evidence | Depends: 3.5] Reject `lifecycle_status: replaced` without an explicit valid replacement URL and reject a non-null replacement under every other lifecycle status; allow `link_status: redirected` with a null replacement.
- [ ] 3.7 [Owner: source-evidence | Depends: 3.6] Add a complete status/date combination matrix proving no link result advances inventory, content, or claim review and no lifecycle judgment advances link/content/claim review.
- [ ] 3.8 [Owner: source-evidence | Depends: 3.7] Add presentation-contract tests for qualified lifecycle/transport labels and prohibit “verified,” “current,” “trusted,” “supported,” or unqualified quality badges.
- [ ] 3.9 [Owner: source-evidence | Depends: 3.8] Add negative fixtures expecting `SOURCE_SCHEMA_INVALID` for legacy `status`/`last_checked` surviving after migration, invalid enums, missing keys, impossible date/replacement combinations, and the string sentinel `none`.

## 4. Build Versioned Per-Citation Claim Review

- [ ] 4.1 [Owner: claim-review | Depends: 3.9] Define stable citation keys for prompt-level (`prompt:<slug>`) and mode-level (`prompt:<slug>:mode:<mode-id>`) citations and reject ambiguous or structurally invalid keys.
- [ ] 4.2 [Owner: claim-review | Depends: 4.1] Implement a versioned prompt-level claim-bearing projection covering the complete user-facing prompt semantics and all modes while excluding source lists, provenance, volatile values, and purely visual/order metadata.
- [ ] 4.3 [Owner: claim-review | Depends: 4.2] Implement a versioned mode-level claim-bearing projection covering shared prompt semantics plus the exact mode's complete user-facing semantics under the same exclusions.
- [ ] 4.4 [Owner: claim-review | Depends: 4.3] Add a contract test that requires every newly introduced user-facing semantic field to enter the projection or deliberately change the projection version.
- [ ] 4.5 [Owner: claim-review | Depends: 4.4] Implement recursively key-sorted, whitespace-free UTF-8 canonical JSON and exact `sha256:<64-lowercase-hex>` claim-review fingerprints over `{ projection_version, source_id, canonical_primary_url, citation_key, citation_title, reviewed_on, claim_projection, support_annotations }`, where `canonical_primary_url` comes from the normalized joined manifest record rather than citation-local or replacement data; preserve array order and lock behavior with fixed known-answer vectors.
- [ ] 4.6 [Owner: claim-review | Depends: 4.5] Add a review-stamping command that prints an audit-friendly before/after plan and updates only explicitly selected citation-review records after maintainer confirmation.
- [ ] 4.7 [Owner: claim-review | Depends: 4.6] Validate that every claim-review key is unique within its source and resolves to a current citation of that exact source and kind.
- [ ] 4.8 [Owner: claim-review | Depends: 4.7] Emit `claim_review: null` for a citation without a review record and never inherit a date or annotation from another citation of the same source.
- [ ] 4.9 [Owner: claim-review | Depends: 4.8] Report `SOURCE_CLAIM_REVIEW_TARGET_INVALID` for duplicate, unknown, removed, wrong-source, wrong-prompt, wrong-mode, or wrong-kind targets with the review location and a current catalog location when one exists.
- [ ] 4.10 [Owner: claim-review | Depends: 4.9] Report `SOURCE_CLAIM_REVIEW_STALE` when recomputed current content or the resolved manifest canonical primary URL does not match an authored fingerprint, and block projection rather than dropping or publishing stale review metadata.
- [ ] 4.11 [Owner: claim-review | Depends: 4.10] Add negative fixtures for prompt semantic edits, mode semantic edits, contextual-title edits, source-id/key edits, normalized canonical-primary-URL edits, projection-version changes, and claim-review date edits without restamping; prove replacement URLs and citation-local compatibility data cannot preserve the old review.
- [ ] 4.12 [Owner: claim-review | Depends: 4.11] Add annotation invalidation fixtures for insertion, deletion, reordering, Unicode normalization changes, and same-calendar-day edits, proving validation is state/fingerprint based rather than Git-history or date-difference based.
- [ ] 4.13 [Owner: claim-review | Depends: 4.12] Add multi-citation fixtures proving one reviewed citation never lends its date or annotations to another prompt or mode citation.
- [ ] 4.14 [Owner: claim-review | Depends: 4.13] After tasks 4.2-4.5 land, stamp accepted and stale claim-review fixtures—including a manifest canonical-primary-URL mutation—exclusively through the production projection/fingerprint implementation, and assert the helper cannot substitute independent hashing or citation-local URL input.

## 5. Validate Manual Annotations and Public Claim Scope

- [ ] 5.1 [Owner: public-scope | Depends: 4.14] Validate support annotations only inside exact per-citation claim-review records as trimmed NFC non-empty plain text of at most 512 Unicode scalar values.
- [ ] 5.2 [Owner: public-scope | Depends: 5.1] Add negative annotation fixtures for leading/trailing whitespace, empty text, control characters, invalid normalization, HTML tags, Markdown links/images, and text exceeding 512 Unicode scalar values.
- [ ] 5.3 [Owner: public-scope | Depends: 5.2] Implement `public_claim_scope` validation for explicit publication approval, reviewed text, non-empty unique coverage, review date, and fingerprint.
- [ ] 5.4 [Owner: public-scope | Depends: 5.3] Require every public-scope coverage key to resolve to this source and to have a current valid per-citation claim review.
- [ ] 5.5 [Owner: public-scope | Depends: 5.4] Compute exact `sha256:<64-lowercase-hex>` public-scope fingerprints with the shared canonical serializer over `{ projection_version, source_id, approved_for_publication, text, reviewed_on, covered_claim_reviews }`, where covered entries are `{ citation_key, review_fingerprint }` objects sorted by citation key and every covered fingerprint already binds the resolved canonical primary URL; add fixed known-answer vectors.
- [ ] 5.6 [Owner: public-scope | Depends: 5.5] Report `SOURCE_PUBLIC_CLAIM_SCOPE_INVALID` for a present record with missing/false approval, malformed/unsafe text, text exceeding 2,000 Unicode scalar values, empty/duplicate/unknown/wrong-source coverage, invalid date, fingerprint syntax, or record shape.
- [ ] 5.7 [Owner: public-scope | Depends: 5.6] Report `SOURCE_PUBLIC_CLAIM_SCOPE_STALE` for fingerprint disagreement after claim, annotation, covered canonical-primary-URL, coverage-set, scope-text, review-date, or projection-version changes; require the target-URL case to flow through current covered claim-review fingerprints.
- [ ] 5.8 [Owner: public-scope | Depends: 5.7] Add negative fixtures proving legacy `claim_scope` and unrestricted `notes` are never copied, summarized, trimmed into publication, or used as fallback public text.
- [ ] 5.9 [Owner: public-scope | Depends: 5.8] Add projection tests proving an absent `public_claim_scope` key emits `claim_scope: null`, a present invalid/unapproved record blocks projection, and a valid approved record emits only approved text, review date, and exact covered citation keys.
- [ ] 5.10 [Owner: public-scope | Depends: 5.9] Add rendering tests that treat scope and annotations as text, label scope as an authored catalog-use note for named citations, and never present it as a source-wide support/quality badge.
- [ ] 5.11 [Owner: public-scope | Depends: 5.10] Extend the explicit review-stamping command to create, update, and re-stamp selected `public_claim_scope` records only after their text, coverage, review date, and current covered claim fingerprints are reviewed.
- [ ] 5.12 [Owner: public-scope | Depends: 5.11] After task 5.5 lands, stamp accepted and stale public-scope fixtures exclusively through the production public-scope projection/fingerprint implementation, using current production-stamped covered claim fingerprints; include a canonical-primary-URL mutation that first invalidates the covered occurrence review and then invalidates public scope after the occurrence is re-stamped.

## 6. Migrate Existing Source Authoring from Evidence

- [ ] 6.1 [Owner: source-migration | Depends: 5.12] Inventory every existing source record's legacy status, `last_checked`, replacement, claim scope, notes, citations, and source-refresh evidence without modifying claims during the inventory pass.
- [ ] 6.2 [Owner: source-migration | Depends: 6.1] Produce a reviewed migration matrix for legacy `checked`, `redirected`, `blocked`, `stale`, `replaced`, and `not checked` values; require a maintainer decision where evidence does not determine independent lifecycle/link states.
- [ ] 6.3 [Owner: source-migration | Depends: 6.2] Add canonical display titles using authored citation titles and recorded source evidence without generated paraphrases or network-derived names.
- [ ] 6.4 [Owner: source-migration | Depends: 6.3] Replace legacy status fields with explicit lifecycle/link values and replace `last_checked` with the three present nullable source-review keys.
- [ ] 6.5 [Owner: source-migration | Depends: 6.4] Use legacy `last_checked` only as inventory-review evidence unless a separate dated record proves link or content review.
- [ ] 6.6 [Owner: source-migration | Depends: 6.5] Replace the string replacement sentinel `none` with null and confirm every editorially replaced source has an explicit canonical replacement URL.
- [ ] 6.7 [Owner: source-migration | Depends: 6.6] Create no per-citation claim review or public claim scope unless a maintainer deliberately reviews the current projection and stamps it.
- [ ] 6.8 [Owner: source-migration | Depends: 6.7] Preserve legacy claim-scope and notes prose only as internal migration evidence or remove it through a separately reviewed authoring decision; never auto-publish it.
- [ ] 6.9 [Owner: source-migration | Depends: 6.8] Rewrite source-refresh documentation and tables to distinguish inventory review, link check, content inspection, claim review, redirect observation, and replacement judgment.
- [ ] 6.10 [Owner: source-migration | Depends: 6.9] Run the migration against a copy/fixture first and inspect every inferred-null and maintainer-decision row before changing canonical authoring data.

## 7. Join Catalog Citations Before Projection

- [ ] 7.1 [Owner: source-join | Depends: 6.10] Accept the source manifest as an explicit catalog-core/CLI input and retain source, prompt, mode, citation, claim-review, and public-scope file/instance locations.
- [ ] 7.2 [Owner: source-join | Depends: 7.1] Build deterministic unique indexes for source id and canonical primary URL before visiting catalog citations; report duplicate ids as `SOURCE_ID_DUPLICATE` with every manifest location.
- [ ] 7.3 [Owner: source-join | Depends: 7.2] Reject canonical primary-URL collisions with `SOURCE_URL_AMBIGUOUS`, listing every source id and manifest location without selecting by file order.
- [ ] 7.4 [Owner: source-join | Depends: 7.3] Join every prompt-level and mode-level citation occurrence to exactly one primary source while retaining its owning context, contextual title, stable citation key, order, and exact claim-review/null state. Reject duplicate same-source citations within one owning array as `SOURCE_CITATION_DUPLICATE` at every duplicate location, but do not deduplicate the same source across prompt-level and mode-level contexts.
- [ ] 7.5 [Owner: source-join | Depends: 7.4] Report `SOURCE_CITATION_UNMATCHED` with authored/normalized URL and exact citation location when no primary source matches.
- [ ] 7.6 [Owner: source-join | Depends: 7.5] Add a replacement-only fixture proving replacement URLs are metadata and never implicit join keys.
- [ ] 7.7 [Owner: source-join | Depends: 7.6] Attach either the current exact claim-review record or null only after join, target, canonical-primary-URL-bound fingerprint, and currentness validation.
- [ ] 7.8 [Owner: source-join | Depends: 7.7] Validate public-scope coverage after all citation/claim indexes exist and before constructing generated source records.
- [ ] 7.9 [Owner: source-join | Depends: 7.8] Accumulate stable source diagnostics in deterministic order and prevent the emitter from receiving any source-invalid package.
- [ ] 7.10 [Owner: source-join | Depends: 0.1, 7.9, 7.11] After task 0.1 records either `independent-base-proven` or `combined-active`, expose deterministic validated source joins and source-specific projection/comparator inputs through the compiled prompt seam without replacing prompt diagnostics or creating a checked-pair writer; in `combined-active` mode this seam exposure grants no independent apply/archive/publication permission.
- [ ] 7.11 [Owner: source-join | Depends: 7.9] Reject source provenance/review/scope fields authored inside prompt or mode citations as `SOURCE_CITATION_FIELDS_INVALID` at their exact citation paths.

## 8. Emit the Full Source Projection and Bounded Companion

- [ ] 8.1 [Owner: source-projection | Depends: 7.10] Increment the full `catalog.json` schema version for the source projection, synchronize `catalog-meta.json` to that supported version, and publish one full-artifact source array ordered by stable source id.
- [ ] 8.2 [Owner: source-projection | Depends: 8.1] Include exactly one generated source for each source reached by at least one prompt or mode citation and exclude uncited README-inventory records.
- [ ] 8.3 [Owner: source-projection | Depends: 8.2] Emit source fields `id`, `display_title`, `aliases`, canonical `url`, `owner`, `source_type`, `lifecycle_status`, `link_status`, `replacement_url`, three required nullable source-review dates, and nullable approved `claim_scope`.
- [ ] 8.4 [Owner: source-projection | Depends: 8.3] Exclude legacy `status`, `last_checked`, notes, source-level claim-review dates, source-level annotations, remote favicon URLs, and runtime-enrichment instructions.
- [ ] 8.5 [Owner: source-projection | Depends: 8.4] Emit each prompt-level and mode-level citation occurrence in its owning ordered array as `{ source_id, title, claim_review }`, where title stays contextual and claim review is null or contains only that occurrence's reviewed date and ordered annotations. A selected-mode composition MUST retain both prompt and selected-mode occurrences, even when their `source_id` values match.
- [ ] 8.6 [Owner: source-projection | Depends: 8.5] Build aliases from distinct trimmed NFC contextual citation titles that differ from the display title, sorted deterministically by Unicode code point without first-file-wins behavior.
- [ ] 8.7 [Owner: source-projection | Depends: 8.6] Add referential-integrity checks proving every generated citation resolves exactly once and every generated source is cited.
- [ ] 8.8 [Owner: source-projection | Depends: 8.7] Keep `catalog-meta.json` free of prompt/source collections while retaining bounded shell metadata and matching full-artifact version/timestamp.
- [ ] 8.9 [Owner: source-projection | Depends: 8.8] Produce deterministic source projection inputs and companion-field expectations only—projection shape, field order, and expected companion fields; the prerequisite change's `site-data-integration` owner alone serializes the final full artifact bytes, computes `catalog_sha256` over those exact bytes, and stores it in the companion before the pair enters the existing commit protocol.
- [ ] 8.10 [Owner: source-projection | Depends: 8.9] Add source-owned tests for deterministic canonical-source and per-context citation ordering, null-key presence, same-source prompt/selected-mode occurrence preservation, canonical-primary-URL review invalidation, and reproducible projection under shuffled source-table and catalog filesystem traversal; exact-byte digest-disagreement and cross-file pair-integrity cases remain owned by prerequisite catalog task 9.6.
- [ ] 8.11 [Owner: source-projection | Depends: 8.10] Supply source-specific comparator expectations, fixtures, and interfaces—source identity, per-context citation occurrences, aliases, provenance, statuses, replacements, dates, claim reviews, public scope, and referential integrity—for consumption by the prerequisite change's shared comparator; its `site-data-integration` owner (catalog task 9.5) alone implements and executes the shared comparator and its digest-first verification, so this task claims no digest-verification ownership.
- [ ] 8.12 [Owner: source-assurance | Depends: 7.10, 8.11] After task 7.10, complete an integration-readiness contract test proving the validated source projection, source-specific comparator expectations, and lossless citation occurrences are independently consumable without writing the checked pair. This readiness result MUST NOT gate the prerequisite change's catalog prompt projection task 9.4.
- [ ] 8.13 [Owner: source-assurance | Depends: 8.12, catalog-contract-completeness-ordering:9.4] After source task 8.12 and `catalog-contract-completeness-ordering` task 9.4, run the final cross-change coexistence contract proving prompt completeness/evaluation/order fields and the source projection survive together, including a same-source prompt/selected-mode citation case with distinct titles and claim reviews; the prerequisite change's task 9.6 then consumes both source results.

## 9. Preserve Transaction Recovery and Failure Atomicity

- [ ] 9.1 [Owner: site-data-integration | Depends: 8.13] Integrate source generation only after the command resolves the target pair, acquires the writer lock, and executes the existing interrupted-journal recovery path.
- [ ] 9.2 [Owner: site-data-integration | Depends: 9.1] Ensure prior recovery reads only validated prior journal metadata and owned transaction artifacts, never newly requested catalog/source input.
- [ ] 9.3 [Owner: site-data-integration | Depends: 9.2] Keep the writer lock after successful recovery while loading and validating the new catalog/source request.
- [ ] 9.4 [Owner: site-data-integration | Depends: 9.3] After recovery establishes the baseline, prevent invalid new source input from causing any new-request journal, staging directory, creation claim, backup, candidate rename, or target write; prior recovery operations are not classified as residue from the new request.
- [ ] 9.5 [Owner: site-data-integration | Depends: 9.4] Define and test the recovered output pair—not the pre-command mixed pair—as the immutability baseline after successful recovery.
- [ ] 9.6 [Owner: site-data-integration | Depends: 9.5] Add a positive recovery fixture where a prior interrupted commit completes, new source validation then fails, recovered output bytes remain stable, and no new transaction residue remains.
- [ ] 9.7 [Owner: site-data-integration | Depends: 9.6] Add recovery-precedence fixtures for initializing, staged, first-rename, second-rename, cleanup, and safely reclaimable dead-owner phases followed by invalid new source input.
- [ ] 9.8 [Owner: site-data-integration | Depends: 9.7] Add unsafe-recovery fixtures proving invalid ownership, paths, pair identity, phases, or metadata report recovery failure before new-input diagnostics and preserve diagnosable evidence.
- [ ] 9.9 [Owner: site-data-integration | Depends: 9.8] Keep the freshness checker non-reclaiming and non-mutating with respect to generated outputs and pre-existing writer/recovery state; allow only its existing serialized-check lock/claim, require clean release without checker-owned residue, and make pending recovery take precedence over catalog/source parsing and validation.
- [ ] 9.10 [Owner: site-data-integration | Depends: 9.9] Add checker fixtures for pending recovery plus invalid source input, clean pair plus invalid input, source semantic drift, digest drift, and paired timestamp-only volatility.
- [ ] 9.11 [Owner: site-data-integration | Depends: 9.10] Preserve the existing fsync barriers, rollback rules, guarded cleanup, signal behavior, lock ownership, and same-directory staging semantics without creating a source-specific transaction path.
- [ ] 9.12 [Owner: site-data-integration | Depends: 9.11] Add residue assertions that distinguish valid prior-recovery effects from prohibited residue attributable to the invalid new request.

## 10. Migrate Static Web and Discovery Consumers to Source IDs

- [ ] 10.1 [Owner: web-source-consumers | Depends: 8.13, 9.12] Add a runtime-validated generated-data boundary that rejects unsupported versions, full/companion digest disagreement, malformed source records, invalid status/date combinations, duplicate/missing source ids, and unresolved citations.
- [ ] 10.2 [Owner: web-source-consumers | Depends: 10.1] Reject generated source-level claim-review fields, source-level annotations, malformed citation claim-review objects, and malformed public-scope projections at runtime.
- [ ] 10.3 [Owner: web-source-consumers | Depends: 10.2] Build one immutable source-id lookup and update prompt source accessors to combine contextual citation titles with canonical global provenance and exact citation review.
- [ ] 10.4 [Owner: web-source-consumers | Depends: 10.3] Change Explore source item identity from URL-derived keys to `source:<source-id>` and remove raw-URL deduplication and encounter-order title selection.
- [ ] 10.5 [Owner: web-source-consumers | Depends: 10.4] Add canonical title and deterministic aliases to the shared deterministic source search fields without introducing fuzzy, hosted, semantic, or AI search.
- [ ] 10.6 [Owner: web-source-consumers | Depends: 10.5] Render independent lifecycle/link labels and dates, nullable inventory/content dates, exact per-citation review, and approved claim scope without collapsed “verified/current/supported” language.
- [ ] 10.7 [Owner: web-source-consumers | Depends: 10.6] Preserve ordinary user-activated external anchors while ensuring source row/dossier inspection performs no source-host, favicon, metadata, proxy, analytics, or AI request.
- [ ] 10.8 [Owner: web-source-consumers | Depends: 10.7] Add accessible labels and explanations that distinguish authored evidence, transport observations, content review, claim review, and public-scope coverage.
- [ ] 10.9 [Owner: seo-discovery | Depends: 0.1, 8.12, catalog-contract-completeness-ordering:9.4] Under the prerequisite change's sole `seo-discovery` owner, and only after task 0.1, source readiness task 8.12, and prerequisite catalog prompt projection task 9.4, migrate `web/scripts/emit-seo.mjs`, `web/scripts/emit-seo.test.mjs`, overlapping SEO and route/discovery adapters, full-Markdown serializers, and every fixture/parity surface for those outputs to the combined prompt/source schema.
- [ ] 10.10 [Owner: seo-discovery | Depends: 10.9] Build one immutable `catalog.sources` lookup for static discovery generation, reject duplicate canonical source ids before rendering, and resolve every prompt-level and mode-level citation `source_id` to exactly one canonical URL without restoring citation-local URL fields.
- [ ] 10.11 [Owner: seo-discovery | Depends: 10.10] Update `llms-full.txt`, overlapping full-Markdown outputs, and any other static SEO or route/discovery citation serialization to retain each prompt-level and mode-level occurrence in its authored section and order, using the occurrence's contextual title and exact `claim_review`/null state. Do not use the source lookup as a `source_id` deduplication set.
- [ ] 10.12 [Owner: seo-discovery | Depends: 8.13, 10.11] After source task 8.13 confirms final coexistence, add focused and parity cases across the overlapping SEO, route/discovery, and full-Markdown outputs for prompt-only, mode-only, unresolved id, duplicate canonical id, shuffled source-table order, and one source cited at both prompt and selected-mode levels with different titles and review states; require both cross-context occurrences and one canonical URL to render.

## 11. Add End-to-End Negative and Regression Assurance

- [ ] 11.1 [Owner: source-assurance | Depends: 9.12, 10.8, 10.12] Add catalog-core unit suites for URL identity, source schema, status/date parity, citation keys, claim projections, canonical-primary-URL-bound fingerprints, annotation safety, public scope, joins, aliases, and diagnostic ordering.
- [ ] 11.2 [Owner: source-assurance | Depends: 11.1] Add CLI tests asserting exact stable codes and per-code locations for schema/status/date failures, duplicate ids, invalid URLs, canonical collisions, unmatched/duplicate citations, unsupported citation fields, malformed/stale claim reviews including canonical target changes, invalid claim targets, and invalid/stale public scope.
- [ ] 11.3 [Owner: source-assurance | Depends: 11.2] Add source-checker regression tests proving current source-refresh tables and README inventory coverage survive centralization without upgrading HTTP evidence.
- [ ] 11.4 [Owner: source-assurance | Depends: 11.3] Add generated-schema/runtime-schema parity tests for all source/citation/public-scope fields and forbidden legacy/runtime-enrichment fields.
- [ ] 11.5 [Owner: source-assurance | Depends: 11.4] Add publisher tests for recovery-before-validation, post-recovery baseline immutability, no-new-transaction residue, unsafe-recovery precedence, paired digest integrity, and crash-consistent source publication.
- [ ] 11.6 [Owner: web-assurance | Depends: 11.5] Add web unit tests for source-id resolution, alias retention, deterministic ordering, null reviews, exact occurrence-owned citation review, canonical-target invalidation, same-source prompt/selected-mode preservation, safe public scope, and qualified status labels.
- [ ] 11.7 [Owner: web-assurance | Depends: 11.6] Add browser tests that inspect source rows/dossiers and selected-mode citation composition, verify contextual/canonical titles and exact review ownership, retain same-source cross-context occurrences, assert no background enrichment requests, and exercise explicit external navigation separately.
- [ ] 11.8 [Owner: web-assurance | Depends: 11.7] Add accessibility and forced-colors coverage for status/review/scope disclosures and ensure non-color text communicates every state.
- [ ] 11.9 [Owner: web-assurance | Depends: 11.8] Add negative tests proving HTTP 200, redirects, non-empty titles, and browser navigation never advance or render content/claim verification.
- [ ] 11.10 [Owner: web-assurance | Depends: 11.9] Add bundle/static-analysis checks proving no runtime source-fetch client, remote favicon service, AI summary text, hidden URL normalizer, duplicated source registry, or semantic-verification copy ships.
- [ ] 11.11 [Owner: seo-discovery | Depends: 11.10] Add static-discovery regression tests proving `emit-seo` rejects unresolved/duplicate source ids, is invariant to canonical source-table traversal order, and preserves prompt-level plus selected-mode citation occurrences and contextual metadata without URL copying or `source_id` deduplication.

## 12. Document, Regenerate, and Validate the Migration

- [ ] 12.1 [Owner: documentation | Depends: 11.11] Update contributor, source-maintenance, generated-data, and architecture documentation to name authoring ownership, review dimensions, citation keys, stamping commands, public-scope approval, and recovery sequencing once.
- [ ] 12.2 [Owner: documentation | Depends: 12.1] Update `AGENTS.md` and `DESIGN.md` where public data shapes, command ownership, or repository workflow changes require it.
- [ ] 12.3 [Owner: generated-publication | Depends: 12.2, catalog-contract-completeness-ordering:9.7] Only after every source implementation and cutover prerequisite in sections 0-11, source documentation tasks 12.1-12.2, source task 8.13, and prerequisite task 9.7 complete all combined projection, consumer, discovery, comparator, and transaction checks, hand the validated projection inputs and inspection checklist to the prerequisite change's `generated-publication` owner. That owner alone regenerates the checked pair through tasks 11.1-11.6 of the prerequisite change and verifies source count, id uniqueness, occurrence coverage, alias ordering, null semantics, canonical-target-bound claim-review attachment, public-scope coverage, and companion digest; this source workstream performs no independent pair write.
- [ ] 12.4 [Owner: source-assurance | Depends: 12.3, catalog-contract-completeness-ordering:11.6] Run source-manifest checks, catalog validation/tests, schema parity, production-backed canonical-target-bound fingerprint/public-text fixtures, static SEO/LLM discovery parity, site-data generation/freshness/recovery suites, and deterministic regeneration checks under pinned Node 24 and pnpm 11.21.0.
- [ ] 12.5 [Owner: release-assurance | Depends: 12.3, catalog-contract-completeness-ordering:11.6] Run web typecheck, focused unit tests including `web/scripts/emit-seo.test.mjs`, browser/network/accessibility suites, static discovery and bundle inspection, format, lint, documentation links, strict OpenSpec validation, and full repository validation.
- [ ] 12.6 [Owner: release-assurance | Depends: 12.3, catalog-contract-completeness-ordering:11.6] Confirm the final implementation changes no product taxonomy or route family and introduces no runtime fetch, remote favicon, hosted/AI search, analytics, AI-generated source prose, or semantic-verification claim.
- [ ] 12.7 [Owner: release-coordination | Depends: 12.4, 12.5, 12.6] After assurance tasks 12.4, 12.5, and 12.6 all pass, coordinate the source schema/data, catalog compiler/emitter integration, sole-owner checked-pair regeneration, runtime/static-discovery consumer migration, tests, and docs as one compatible release unit; do not ship mixed schema versions or duplicate publication ownership.
- [ ] 12.8 [Owner: graph-assurance | Depends: 12.7] Run the parseable source-task DAG closeout oracle and refuse closeout unless every source task except 12.8 is a transitive ancestor of 12.8, every dependency target exists, no cycle exists, catalog task 9.4 is not gated by source task 8.12, source task 8.13 joins after both, and sole-writer ownership remains intact.
