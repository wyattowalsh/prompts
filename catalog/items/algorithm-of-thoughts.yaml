slug: algorithm-of-thoughts
title: Algorithm-of-Thoughts
facet: method
lane: reasoning
blurb: guide solving with an explicit algorithmic search strategy.
order: 150
badge:
  logo: ri:RiFunctionLine
  color: 8B5CF6
  chip_label: AoT
evidence: Emerging; primary research.
safety:
  - The template is a lightweight approximation of a search procedure.
  - Prefer an external solver or executable representation when available.
caveat: the template is a lightweight approximation of a search procedure.
definition: guide solving with an explicit algorithmic search strategy.
avoid_when: no clear algorithm exists or examples are too complex to fit.
model_api_controls: >-
  prefer external solver or executable representation when available. Distinct from
  provider thinking budgets; algorithm-style decomposition still needs evals and may be
  replaceable by reasoning APIs on simple tasks.
cost_latency: moderate.
failure_modes: shallow search, state-tracking errors, false confidence.
eval_required: true
sources:
  - title: Algorithm of Thoughts
    url: https://arxiv.org/abs/2308.10379
  - title: OpenAI reasoning guide
    url: https://developers.openai.com/api/docs/guides/reasoning
modes:
  - id: template
    label: Method template
    default: true
    when_to_use: constrained problem solving where the algorithm is known.
    placeholders:
      - name: problem
        required: true
        example: Train A 9:00 at 60mph; Train B 10:00 at 90mph — when meet?
        notes: Problem to solve
    prompt: |
      Problem:
      <input>
      {problem}
      </input>

      Use this strategy:
      1. Represent the state.
      2. Generate candidate moves.
      3. Score candidates against the objective.
      4. Continue until solved or blocked.

      Return the final answer, concise search summary, and checks.
